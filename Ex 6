from itertools import combinations

def get_combinations(s, n):
    out = []
    for i in range(n):
        out += sorted(list(map(''.join, map(sorted, combinations(s, i+1)))))
    return out

print(get_combinations("cat", 2))
